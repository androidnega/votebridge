from django.apps import AppConfig


class TrustedDevicesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.trusted_devices"
    verbose_name = "Trusted Devices & Risk-Based Authentication"
